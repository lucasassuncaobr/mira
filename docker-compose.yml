services:
  mira:
    image: node:20-alpine
    container_name: mira
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:3000"
    command: node server.js
    restart: unless-stopped
